overwrite: true
schema: "./src/contentful/types/contentful-schema.graphql"
documents: "src/**/*.graphql"
generates:
    src/generated/contentful-schema-types.tsx:
        plugins:
            - "typescript"
